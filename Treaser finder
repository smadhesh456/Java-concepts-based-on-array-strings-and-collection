import java.io.*;
import java.util.*;

public class Solution {

    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int a = sc.nextInt();
        int b = sc.nextInt();
        int c = sc.nextInt();
        if ((a > b && a < c) || (a < b && a > c)) {
            System.out.println("The treasure is in the box which has number " + a + ".");
        } 
        else if ((b > a && b < c) || (b < a && b > c)) {
            System.out.println("The treasure is in the box which has number " + b + ".");
        } 
        else {
            System.out.println("The treasure is in the box which has number " + c + ".");
        }

         
       if(b%a==0||c%a==0){ 
                System.out.print("The code to open the box is "+a+".");
            }
        else if(a%b==0 || c%b == 0){
            System.out.print("The code to open the box is "+b+".");
        }
        else{
            System.out.print("The code to open the box is "+c+".");
        }
        }
    }
